<div class="d-flex flex-column justify-content-center align-items-center">
    <h1>Login</h1>
    <% if(typeof reg == "boolean") {%>
        <div class="alert alert-success  d-flex align-items-center fade show" role="alert">
            <div class="mx-auto">
                <p class="text-center">Registration complete! You can now log in using your details.</p>
            </div>
            <button type="button" class="btn-close fade show" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <%}%>
    
    <% if(typeof error !== "undefined") {%>
    <div class="alert alert-warning d-flex align-items-center" role="alert">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor"
            class="bi bi-exclamation-triangle-fill flex-shrink-0 me-2" viewBox="0 0 16 16" role="img"
            aria-label="Warning:">
            <path
                d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z" />
        </svg>
        <div>
            <%= error %>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    <%}%>

    <% if(typeof message !== "undefined" && message.length > 0 ) {%>

        <%= "🔻 "+message %>

    <%}%>
    <form action="/login" method="POST" class="loginForm">


        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" class="form-control primaryElem"
                placeholder="Enter Email" autocomplete="username" required     />
        </div>
        <div class="form-group">
            <label for="password">Password</label>
            <input type="password" id="password" name="password" class="form-control primaryElem"
                placeholder="Enter Password" autocomplete="current-password" required/>
        </div>
        <button type="submit" class="primaryElem w-100 mt-5 dodgerblue">Login</button>
    </form>
</div>